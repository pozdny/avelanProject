<fieldset>
    <legend>{$legend}</legend>
    <div id="{$add_input_id}" {$add_input_class}></div>
    <input name="add_id" id="add_id" type="hidden" value="{$add_id}">
    {$hidden_add_img}
    <button type="button" class="btn btn-success" id="{$button_add_input}">Добавить поле</button>
    {$submit_button}
</fieldset>